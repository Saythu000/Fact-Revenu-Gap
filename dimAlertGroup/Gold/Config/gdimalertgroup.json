{
  "SubLayerProcessing": [
    {
      "SubGroupEntity": "dimAlertGroup",
      "DestinationTable": "claimsprocessing.gold.gold_dimalertgroup",
      "SourceTables": [
        {
          "Entity": "alertGroup",
          "SourceTable": "claimsprocessing.silver.silver_alertgroup",
          "SourceFormat": "delta"
        }
      ],
      "SQLScript": "SELECT alert_group_id AS alertGroupKey, alert_group_code AS alertGroupCode, alert_group_description AS alertGroupDescription, display_text AS displayText, sort_order AS sortOrder, is_active AS isActive FROM alertGroup",
      "MergeScript": "MERGE INTO claimsprocessing.gold.gold_dimalertgroup t USING (SELECT * FROM tempSQLScript) s ON s.alertGroupKey = t.alertGroupKey WHEN MATCHED THEN UPDATE SET alertGroupCode = s.alertGroupCode, alertGroupDescription = s.alertGroupDescription, displayText = s.displayText, sortOrder = s.sortOrder, isActive = s.isActive WHEN NOT MATCHED THEN INSERT (alertGroupKey, alertGroupCode, alertGroupDescription, displayText, sortOrder, isActive) VALUES (s.alertGroupKey, s.alertGroupCode, s.alertGroupDescription, s.displayText, s.sortOrder, s.isActive)"
    }
  ]
}

